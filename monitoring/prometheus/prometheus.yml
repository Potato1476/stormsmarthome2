global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'storm-exporter'
    static_configs:
      # Placeholder — thay bằng EIP thật của máy cloud trước khi chạy:
      #   sed -i '' "s/CLOUD_PUBLIC_IP/<ip>/" monitoring/prometheus/prometheus.yml
      - targets: ['CLOUD_PUBLIC_IP:8000']
